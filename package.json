{
  "name": "l2-harvest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",

    "deploy-matic": "graph codegen && graph build --network matic && graph deploy --product hosted-service alexandersazonof/l2-test",
    "deploy-arbitrum": "graph codegen && graph build --network arbitrum-one && graph deploy --product hosted-service alexandersazonof/l2-arb",
    "deploy-base": "graph codegen && graph build --network base && graph deploy --studio -l v0.0.1 l2-base",

    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
