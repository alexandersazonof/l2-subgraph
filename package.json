{
  "name": "l2-harvest",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",

    "deploy-matic": "graph codegen && graph build --network matic && graph deploy --studio -l v0.0.2 l2-polygon-test",
    "deploy-arbitrum": "graph codegen && graph build --network arbitrum-one && graph deploy --studio -l v0.0.4 l2-arb-test",
    "deploy-base": "graph codegen && graph build --network base && graph deploy --studio -l v0.0.3 l2-base",
    "deploy-zksync-era": "graph codegen && graph build --network zksync-era && graph deploy --studio -l v0.0.4 l2-zksync-era",

    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.61.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
